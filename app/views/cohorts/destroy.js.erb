(function(){
  var cohortsContainer = document.querySelector("#cohorts");

  var deletedElement = cohortsContainer.querySelector("#<%= "cohort-#{@cohort.id}" %>");

  cohortsContainer.removeChild(deletedElement);
})();