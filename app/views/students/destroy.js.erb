(function(){
  var studentsContainer = document.querySelector("#students");

  var deletedElement = studentsContainer.querySelector("#<%= "student-#{@student.id}" %>");

  studentsContainer.removeChild(deletedElement);
})();