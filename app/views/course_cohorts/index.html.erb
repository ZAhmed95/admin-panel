<%= content_for :controls do %>
  <h1>Controls for cohorts:</h1>
  <h3> <%= link_to 'Back to course', course_path(@course) %> </h3>
<% end %>

<%= content_for :display do %>
  <h1>All Cohorts for <%= @course.course_name %>:</h1>
  <div class="container" id="cohorts">
    <%= render partial: 'cohorts/list' %>
  </div>
<% end %>